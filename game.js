(()=>{const c=document.getElementById('game'),x=c.getContext('2d'),overlay=document.getElementById('overlay'),start=document.getElementById('start'),hpEl=document.getElementById('hp'),scoreEl=document.getElementById('score');let running=false,last=0,spawn=0,score=0,hp=100,keys={},mouse={x:480,y:270,down:false},enemies=[],shots=[];const player={x:480,y:270,r:14,speed:220};function reset(){score=0;hp=100;enemies=[];shots=[];player.x=480;player.y=270;spawn=0;hpEl.textContent='체력 100';scoreEl.textContent='점수 0'}function pos(e){const r=c.getBoundingClientRect();return{x:(e.clientX-r.left)*c.width/r.width,y:(e.clientY-r.top)*c.height/r.height}}addEventListener('keydown',e=>keys[e.key.toLowerCase()]=true);addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);c.addEventListener('mousemove',e=>Object.assign(mouse,pos(e)));c.addEventListener('mousedown',()=>mouse.down=true);addEventListener('mouseup',()=>mouse.down=false);start.addEventListener('click',()=>{reset();overlay.hidden=true;running=true;last=performance.now();requestAnimationFrame(loop)});function loop(t){if(!running)return;const dt=Math.min((t-last)/1000,.033);last=t;update(dt);draw();if(hp<=0){running=false;overlay.hidden=false;overlay.querySelector('h1').textContent=`기록 ${score}점`;overlay.querySelector('p').textContent='거리를 확보하고 적이 가까워지기 전에 방향을 바꿔 보세요.';start.textContent='다시 도전';return}requestAnimationFrame(loop)}function update(dt){let dx=(keys.d?1:0)-(keys.a?1:0),dy=(keys.s?1:0)-(keys.w?1:0),n=Math.hypot(dx,dy)||1;player.x=Math.max(15,Math.min(c.width-15,player.x+dx/n*player.speed*dt));player.y=Math.max(15,Math.min(c.height-15,player.y+dy/n*player.speed*dt));spawn-=dt;if(spawn<=0){const side=Math.floor(Math.random()*4),p=side<2?Math.random()*c.width:Math.random()*c.height;enemies.push({x:side===0?p:side===2?0:side===3?c.width:p,y:side===0?0:side===1?c.height:side===2?p:p,r:13,s:65+Math.min(score,100)});spawn=Math.max(.28,1-score/800)}if(mouse.down&&(!shots.cool||shots.cool<=0)){const a=Math.atan2(mouse.y-player.y,mouse.x-player.x);shots.push({x:player.x,y:player.y,vx:Math.cos(a)*520,vy:Math.sin(a)*520,r:4});shots.cool=.16}shots.cool=(shots.cool||0)-dt;shots.forEach(b=>{b.x+=b.vx*dt;b.y+=b.vy*dt});shots=shots.filter(b=>b.x>-10&&b.x<c.width+10&&b.y>-10&&b.y<c.height+10);enemies.forEach(e=>{const a=Math.atan2(player.y-e.y,player.x-e.x);e.x+=Math.cos(a)*e.s*dt;e.y+=Math.sin(a)*e.s*dt;if(Math.hypot(player.x-e.x,player.y-e.y)<player.r+e.r){hp-=28*dt;hpEl.textContent=`체력 ${Math.max(0,Math.ceil(hp))}`}});for(let i=enemies.length-1;i>=0;i--)for(let j=shots.length-1;j>=0;j--)if(Math.hypot(enemies[i].x-shots[j].x,enemies[i].y-shots[j].y)<enemies[i].r+shots[j].r){enemies.splice(i,1);shots.splice(j,1);score+=10;scoreEl.textContent=`점수 ${score}`;break}}function draw(){x.fillStyle='#080c11';x.fillRect(0,0,c.width,c.height);x.strokeStyle='#172231';x.lineWidth=1;for(let i=0;i<c.width;i+=48){x.beginPath();x.moveTo(i,0);x.lineTo(i,c.height);x.stroke()}for(let i=0;i<c.height;i+=48){x.beginPath();x.moveTo(0,i);x.lineTo(c.width,i);x.stroke()}x.fillStyle='#f0b95b';x.beginPath();x.arc(player.x,player.y,player.r,0,7);x.fill();x.fillStyle='#ffe4a6';shots.forEach(b=>{x.beginPath();x.arc(b.x,b.y,b.r,0,7);x.fill()});x.fillStyle='#ed6a5a';enemies.forEach(e=>{x.beginPath();x.arc(e.x,e.y,e.r,0,7);x.fill()})}})();
